#include<stdio.h>
struct student {
    char name[20];
    int regno;
};
int main(){
    struct student s={"eswar",1001};
    struct student *point=&s;
    printf("My name is %s\n",point->name);
    printf("My name's memory address is %p\n",point->name);
    printf("My regno is %d\n",point->regno);
    printf("My regno memory address is %p\n",point->regno);
    return 0;
}